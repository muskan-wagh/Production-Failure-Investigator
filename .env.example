# Local-first defaults. Copy to .env for local runs (never commit .env).
# No AWS credentials required.
API_PORT=3001
OLLAMA_MODEL=llama3.1
OLLAMA_BASE_URL=http://localhost:11434
NEXT_PUBLIC_API_URL=http://localhost:3001
